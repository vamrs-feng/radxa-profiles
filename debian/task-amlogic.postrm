#!/bin/sh
set -e

#DEBHELPER#

case $1 in
    remove)
        conflicts="kdeglobals"
        for i in $conflicts
        do
            dpkg-divert --package task-amlogic --rename --remove "/usr/share/desktop-base/kf5-settings/$i"
        done
        ;;
esac

exit 0