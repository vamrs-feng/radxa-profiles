#!/bin/sh
set -e

case $1 in
    install|upgrade)
		dpkg-divert --package rockchip-pulseaudio-config --divert "//etc/pulse/default.pa.bak" --rename "/etc/pulse/default.pa"
        ;;
esac
    
#DEBHELPER#

exit 0